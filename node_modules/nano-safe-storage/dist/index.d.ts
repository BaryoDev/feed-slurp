interface StorageOptions {
    /** Key prefix for namespacing (default: '') */
    prefix?: string;
    /** Default TTL in seconds (optional) */
    ttl?: number;
}
interface SetOptions {
    /** TTL for this specific item in seconds */
    ttl?: number;
}
/**
 * Creates a namespaced storage instance with optional TTL support.
 */
declare function createStorage(options?: StorageOptions): {
    /**
     * Sets a value in storage with optional TTL.
     */
    set<T>(key: string, value: T, setOptions?: SetOptions): void;
    /**
     * Gets a value from storage. Returns null if expired or not found.
     */
    get<T>(key: string): T | null;
    /**
     * Removes an item from storage.
     */
    remove(key: string): void;
    /**
     * Checks if a key exists and is not expired.
     */
    has(key: string): boolean;
    /**
     * Clears all items with the current prefix.
     */
    clear(): void;
    /**
     * Checks if storage is available (not using memory fallback).
     */
    isAvailable(): boolean;
};
declare const setItem: <T>(key: string, value: T, setOptions?: SetOptions) => void;
declare const getItem: <T>(key: string) => T | null;
declare const removeItem: (key: string) => void;
declare const has: (key: string) => boolean;
declare const clear: () => void;

export { type SetOptions, type StorageOptions, clear, createStorage, createStorage as default, getItem, has, removeItem, setItem };
